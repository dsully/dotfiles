complete -c git-review -a create  -d "Create a ReviewBoard with the current commits."
complete -c git-review -a dcommit -d "Pull in ReviewBoard approvals to the last commit."
complete -c git-review -a update  -d "Update the ReviewBoard with new changes."
